---
alwaysApply: true
---

# MCP

## chrome-devtools: 自行阅读和操控酒馆网页

你应该用 chrome-devtools 连接我已经打开的浏览器, 从中读取或操纵连接到的酒馆网页 (其网址与 `.vscode/launch.json` 中配置的 `url` 一致), 来了解当前的界面、脚本情况, 如获取当前的 DOM 情况、实际显示情况、Console 情况、点击界面……

<<<<<<< HEAD
### 如何用 chrome-devtools-mcp 访问酒馆

1. **先启动酒馆**：确保 SillyTavern（或本机酒馆）已运行，且 `http://127.0.0.1:8000/` 可访问（与 `.vscode/launch.json` 中的 `url` 一致）。
2. **用远程调试方式打开 Chrome**：在项目根目录执行  
   `powershell -ExecutionPolicy Bypass -File scripts/launch-chrome-for-mcp.ps1`  
   会启动带 `--remote-debugging-port=9222` 的 Chrome 并打开酒馆页面；保持该 Chrome 窗口不关闭。
3. **在 Cursor 中连接**：`.cursor/mcp.json` 已配置 `chrome-devtools-mcp` 且带 `--autoConnect`，会自动连接本机 9222 端口。连接成功后即可通过 chrome-devtools 工具读取/操控该酒馆网页。

=======
>>>>>>> ca4c8b1b9f4d81aabc8d22440a4a180fca5f4409
### 检查界面、脚本热重载

打开网页后, 你需要检查 `$('#extensions_settings')` 中的`酒馆助手-实时监听-允许监听`开关是否处于启用状态. 一旦启用, 则界面、脚本代码到酒馆网页的实时同步已经建立好了: 在代码变更后, 酒馆网页上将热重载新的脚本或界面代码, 因此你不需要刷新酒馆网页, 也不需要自己运行 `pnpm build` 来更新代码打包结果, 直接查看网页即可.
